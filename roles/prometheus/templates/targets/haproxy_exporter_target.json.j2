[
  {
    "targets": [
      {%- for item in groups.couchdb2_proxy -%}
      "{{ hostvars[item].alt_hostname | default(hostvars[item].hostname) }}:9101"{% if not loop.last %},{% endif %}
      {%- endfor -%} 
    ],
    "labels": {
      "job": "haproxy_exporter",
      "service": "couchdb2_proxy"
    }
  }
]

