[
  {
    "targets": ["{{ hostvars[groups.airflow.0].alt_hostname | default(hostvars[groups.airflow.0].hostname) }}:9112"],
    "labels": {
      "job": "kafka_exporter",
      "service": "kafka"
    }
  }
]

