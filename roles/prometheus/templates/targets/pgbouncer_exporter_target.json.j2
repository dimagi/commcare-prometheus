[
  {
    "targets": [
      {%- for item in groups.postgresql -%}
      {% if not loop.last %}
      "{{ hostvars[item].alt_hostname | default(hostvars[item].hostname) }}:9132",
      {% else %}
      "{{ hostvars[item].alt_hostname }}:9132"
      {% endif %}
      {%- endfor -%} 
    ],
    "labels": {
      "job": "postgres_exporter"
    }
  }
]

