[
  {
    "targets": [
      {%- for item in groups.pg_citusdb_master -%}
      "{{ hostvars[item].alt_hostname | default(hostvars[item].hostname) }}:9187"{% if not loop.last %},{% endif %}
      {%- endfor -%} 
    ],
    "labels": {
      "job": "postgres_exporter",
      "service": "citusdb"
    }
  },

  {
    "targets": [
      {%- for item in groups.pg_citusdb_standby -%}
     "{{ hostvars[item].alt_hostname | default(hostvars[item].hostname) }}:9187"{% if not loop.last %},{% endif %}
      {%- endfor -%} 
    ],
    "labels": {
      "job": "postgres_exporter",
      "service": "pg_standby"
    }
  }
]

