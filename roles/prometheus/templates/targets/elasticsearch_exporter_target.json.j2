[
  {
    "targets": [
      {%- for item in groups.elasticsearch -%}
      {% if not loop.last %}
      "{{ hostvars[item].alt_hostname | default(hostvars[item].hostname) }}:9108",
      {% else %}
      "{{ hostvars[item].alt_hostname }}:9108"
      {% endif %}
      {%- endfor -%} 
    ],
    "labels": {
      "job": "elasticsearch_exporter",
      "service": "elasticsearch"
    }
  }
]

