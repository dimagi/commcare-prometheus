[
  {
    "targets": ["{{ hostvars[groups.zookeeper.0].alt_hostname | default(hostvars[groups.zookeeper.0].hostname) }}:9141"],
    "labels": {
      "job": "zookeeper_exporter",
      "environment": "{{ prometheus_monitoring_env }}",
      "service": "zookeeper"
    }
  }
]
