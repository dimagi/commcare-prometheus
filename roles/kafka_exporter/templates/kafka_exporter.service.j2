[Unit]
Description=Kafka Exporter
After=network.target

[Service]
ExecStart={{ kafka_exporter_symlink }} {% for kafka_server in groups.kafka %}--kafka.server={{ kafka_server }}:9092 {% endfor %} --web.listen-address={{ kafka_exporter_listen_address }}

[Install]
WantedBy=multi-user.target
