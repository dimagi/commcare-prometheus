[Unit]
Description=Grafana loki server
After=network.target

[Service]
Type=simple
ExecStart={{ loki_server_binary }} -config.file="{{ loki_server_config_file }}"

Restart=always
RestartSec=5
TimeoutSec=5

[Install]
WantedBy=multi-user.target
